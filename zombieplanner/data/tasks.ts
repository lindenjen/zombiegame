import {Survivor} from "../classes/survivor";

//eventually this will be the possible tasks a survivor could be assigned
export const tasks = ["scavenge", "fortify", "rest"];

//this will show all the possible outcomes and the user will randomly be given one
export function getTaskOutcome(task: string, survivor: Survivor) {
    if (task === "scavenge") {

    }
}
