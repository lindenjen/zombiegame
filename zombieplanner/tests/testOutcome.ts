import {SafeHouse} from "../classes/safeHouse";
import {Survivor} from "../classes/survivor";

export function checkGameOverConditions(safeHouse: SafeHouse, survivors: Survivor[]): boolean {
    // check to see if safeHouse is OK
    // check to see if all survivors are dead
    // return true if game should end
    return true;
}